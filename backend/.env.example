# Example environment variables
# Put these in a .env file or set in your environment

# PostgreSQL URL format (works with Neon):
# postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql://user:password@localhost:5432/tasks_db

# Optional: JWT secret used to verify HS256 tokens. If not set, token string is used as user id for dev convenience.
JWT_SECRET=your-jwt-secret

# When running with docker-compose, you can set DATABASE_URL like below to connect to the PG service
# DATABASE_URL=postgresql://postgres:postgres@db:5432/tasks_db
